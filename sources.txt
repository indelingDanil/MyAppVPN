# Список подписок для фильтрации — по одному URL на строку.
# Строки, начинающиеся с #, и пустые строки игнорируются.
# Поддерживается plain-текст (vless:// ... по строкам) и base64-подписки.

# На время тестов — только источник 26 (обход по SNI/CIDR, ~98% Reality,
# российские SNI типа max.ru/ya.ru). fetch.sh нормализует github.com/raw -> raw.
https://raw.githubusercontent.com/AvenCores/goida-vpn-configs/main/githubmirror/26.txt
