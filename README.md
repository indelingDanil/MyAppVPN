# MyAppVPN Beta ProxyList — авто-фильтр VLESS-подписок

> ⚠️ **БЕТА-ТЕСТ.** Проект в стадии обкатки. Список и результаты нестабильны,
> флаги/формат вывода могут меняться. Используйте на свой риск.

Дополнение к проекту **[MyAppVPN](https://github.com/indelingDanil/MyAppVPN)**.
Живёт в отдельной ветке `proxylist` того же репозитория.

GitHub Actions раз в час скачивает несколько публичных подписок (тысячи конфигов),
отсеивает мусор и публикует короткий список рабочих **VLESS**-прокси, которые:

1. **живы и открывают Telegram** через саму проксю;
2. проходят **грубый порог скорости** (≥ 1 МБ/с = 8 Мбит/с).

Публикуется только `vless://` — приложение MyAppVPN понимает именно этот протокол
(в стороннем клиенте вроде v2rayN список тоже откроется). Точный замер скорости вы
делаете сами локально в клиенте — см. ниже.

## Как это работает

- **Runner** GitHub Actions — это полноценная Linux-VM. На ней ставится
  [`xray-knife`](https://github.com/lilendian0x00/xray-knife) (standalone-бинарь
  со встроенным xray/sing-box ядром) и гоняет каждый конфиг как реальный туннель.
- **Pass A** (быстро): `xray-knife http -u https://web.telegram.org/` — конфиг
  проходит, только если Telegram реально открывается через него. Заодно
  отсекаются все мёртвые. ~тысячи → сотни.
- **Pass B** (по выжившим): `--speedtest` через `speed.cloudflare.com`, оставляем
  тех, у кого download ≥ 8 Мбит/с. Результат сортируется по скорости, берётся
  топ-300.
- Итог коммитится в [`output/`](output/) в ветке `proxylist`.

### Почему это гибрид (важно про скорость)

Runner живёт в дата-центре (обычно США), поэтому цифра скорости оттуда —
**приблизительная**, не из вашего ISP. GitHub делает грубый отсев; **финальный
точный замер — локально**: после обновления подписки нажмите правый клик по группе
→ «тест скорости». Проверка «жив / Telegram доступен» от локации не зависит и ценна
сама по себе.

## Как пользоваться

**Для приложения MyAppVPN** — используйте **plain-список** (приложение ждёт
текст с литеральными `vless://`, base64 оно на этапе загрузки не принимает):

```
https://raw.githubusercontent.com/indelingDanil/MyAppVPN/proxylist/output/filtered.txt
```

**Для v2rayN и подобных** — можно base64-подписку:

```
https://raw.githubusercontent.com/indelingDanil/MyAppVPN/proxylist/output/filtered_b64.txt
```

- `output/filtered.txt` — plain-список (**основной URL для MyAppVPN**).
- `output/filtered_b64.txt` — тот же список в base64 (для клиентов-подписок).
- `output/stats.md` — сколько было / прошло Telegram / прошло по скорости.

Затем: обновить подписку → правый клик по группе → **тест скорости**.

## Настройка

- **Источники** — редактируйте [`sources.txt`](sources.txt) (по одному URL на
  строку, `#` — комментарий). Поддерживается plain и base64.
- **Параметры** пайплайна задаются переменными окружения (см. значения по
  умолчанию в `scripts/`): `MIN_MBPS` (порог, Мбит/с), `CAP` (размер топа),
  `TG_URL`, `MAX_DELAY`, `PASS_A_THREADS`, `PASS_B_THREADS`.
- **Расписание** — cron в `.github/workflows/check.yml` (сейчас раз в час) +
  ручной запуск кнопкой *Run workflow*.
  - ⚙️ **Важно:** GitHub запускает `schedule` только для workflow-файла из
    **ветки по умолчанию** (`main`). Поэтому сам workflow лежит в `main`, но
    делает `checkout` и коммит **в ветку `proxylist`** — история `main` при этом
    остаётся чистой.

## Оговорки / риски

- Скорость с GitHub ≠ дома (runner в дата-центре). Отсюда гибрид — финальный
  замер локально.
- Многие конфиги фронтятся через Cloudflare — доступность Telegram зависит от
  реального выхода, поэтому тестируем именно через проксю.
- Часовые коммиты создают шум в истории ветки `proxylist` (это ок; `[skip ci]` в
  сообщении не даёт петли запусков).
- GitHub отключает `schedule` после ~60 дней без активности в репо — при простое
  включите workflow заново вручную.

## Дисклеймер

Проект создан **в образовательных и исследовательских целях** — демонстрация
автоматизированного тестирования сетевых конфигураций. Автор **не несёт
ответственности** за использование инструмента и публикуемых списков, а также за
любой ущерб, прямой или косвенный.

Подписки берутся из **публичных** источников; автор их не создаёт, не хостит и не
контролирует их содержимое и работоспособность.

В Российской Федерации использование VPN-сервисов и средств обхода блокировок
**юридически ограничено**. Вы используете этот инструмент **на свой страх и риск**
и обязаны соблюдать законодательство своей юрисдикции.

Лицензия: [MIT](LICENSE).
